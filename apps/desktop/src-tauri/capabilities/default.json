{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Memory desktop grants only explicit folder-selection, scan, watch, and open/reveal commands.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default", "opener:default"]
}
